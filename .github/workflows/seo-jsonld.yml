name: SEO / JSON-LD

on:
  schedule:
    # Diariamente às 06:00 BRT (09:00 UTC) — após o ciclo de deploy noturno.
    - cron: "0 9 * * *"
  # Pull requests are validated against their own local production preview
  # inside build-validate. This workflow validates deployed production only.
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      base_url:
        description: "URL base para validar (default: produção)"
        required: false
        default: "https://0web.com.br"
      with_validator:
        description: "Também rodar Schema.org Validator (mais lento)"
        required: false
        default: "true"

jobs:
  validate:
    name: Validate /servicos JSON-LD
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Rodar validador JSON-LD
        env:
          BASE_URL: ${{ github.event.inputs.base_url || 'https://0web.com.br' }}
          WITH_VALIDATOR: ${{ github.event.inputs.with_validator || 'true' }}
        run: |
          set -e
          if [ "$WITH_VALIDATOR" = "true" ]; then
            node scripts/validate-jsonld.mjs "$BASE_URL" --with-validator
          else
            node scripts/validate-jsonld.mjs "$BASE_URL"
          fi

      - name: Rodar validador de sitemap/robots
        env:
          BASE_URL: ${{ github.event.inputs.base_url || 'https://0web.com.br' }}
        run: node scripts/validate-sitemaps.mjs "$BASE_URL"

      - name: Publicar relatório
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: seo-report-${{ github.run_id }}
          path: seo-reports/
          if-no-files-found: ignore
          retention-days: 30
