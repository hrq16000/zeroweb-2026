import { createFileRoute } from "@tanstack/react-router";
import { z } from "zod";
import { recordPortfolioVital } from "@/lib/portfolio-vitals.server";
const schema = z.object({ name: z.enum(["LCP", "CLS", "INP"]), value: z.number().finite().nonnegative().max(120000), id: z.string().max(80), slug: z.string().regex(/^[a-z0-9][a-z0-9_-]{0,80}$/), path: z.string().max(200), ts: z.number().optional() });
export const Route = createFileRoute("/api/public/portfolio-vitals")({ server: { handlers: { POST: async ({ request }) => { try { const payload = schema.parse(await request.json()); recordPortfolioVital(payload); try { const { supabaseAdmin } = await import("@/integrations/supabase/client.server"); await (supabaseAdmin as any).from("portfolio_web_vitals").insert({ slug: payload.slug, metric: payload.name, value: payload.value, metric_id: payload.id, path: payload.path }); } catch (error) { console.warn("[portfolio-vitals] durable ingest unavailable", (error as Error).message); } return new Response(null, { status: 204 }); } catch { return new Response("Invalid payload", { status: 400 }); } }, GET: async () => new Response("Not found", { status: 404 }) } } });
